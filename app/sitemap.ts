import type { MetadataRoute } from 'next';const routes=['','/menu','/order','/lunch-specials','/about','/contact','/location-hours','/gallery','/catering','/gift-cards','/careers'];export default function sitemap():MetadataRoute.Sitemap{return routes.map(route=>({url:`https://example.com${route}`,lastModified:new Date(),changeFrequency:'weekly',priority:route===''?1:.8}))}
